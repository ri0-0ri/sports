<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.chat.ChatMapper">

	<!-- 팀별 채팅 메시지 조회 -->
	<select id="getAllMessages" resultType="com.example.demo.model.chat.ChatDTO">
		SELECT * FROM fboard
		WHERE team_id = #{teamId}
		ORDER BY created_at DESC
	</select>

	<!-- 새로운 채팅 메시지 저장 -->
	<insert id="insertMessage"
		parameterType="com.example.demo.model.chat.ChatDTO">
		INSERT INTO fboard (user_id, content, team_id)
		VALUES (#{userId}, #{content}, #{teamId})
	</insert>

</mapper>
