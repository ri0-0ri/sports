<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.chat.ChatMapper">

<select id="getchatBygwnum">
	select * from fboard where gWnum=#{gwnum} and chat_type=#{chat_type}
</select>

<select id="getchatBystr">
	select * from fboard where gWnum=#{gwnum} and content=#{str}
</select>

</mapper>
