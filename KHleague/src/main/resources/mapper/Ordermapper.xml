<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.payment.Ordermapper">
	
	<insert id="putorder">
		insert into orders(deliveryPlace, deliveryMemo, totalPrice, sudannum, userid, goodsnums)
		values(#{deliveryPlace}, #{deliveryMemo}, #{totalPrice}, #{sudannum}, #{userid}, #{goodsnums})
	</insert>

</mapper>