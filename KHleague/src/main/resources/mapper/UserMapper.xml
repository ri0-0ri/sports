<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.user.UserMapper">

   <insert id="insertUser">
    INSERT INTO user (userid, username, userbirth, usergender, userphone, userpw, useraddr, userhomenum, userjoin)
    VALUES (#{userid}, #{username}, #{userbirth}, #{usergender}, #{userphone}, #{userpw}, #{useraddr}, #{userhomenum}, #{userjoin});
</insert>

</mapper>